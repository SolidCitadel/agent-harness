---
name: self-improve
description: 사용자가 Claude Code가 요구 공백을 임의로 채웠거나 질문·검수 없이 성급하게 구현했거나 기존 상태·권한·완료 근거를 확인하지 않았다고 지적할 때 사용한다. 일반적인 작업 방식 또는 규칙 적용이 목적을 해친 경우도 포함한다. 원 사건의 최초 근거 없는 확정 지점과 누락된 조사·합의·생산·검증 게이트를 추적하고 agent-harness의 담당 규칙 또는 skill을 개선한다.
---

# 자기 개선

## 진단

1. 원 사건과 관련 문서·코드의 실제 상태를 대조한다.
2. 결과가 잘못된 마지막 시점이 아니라 최초로 근거 없는 결정을 확정한 시점을 찾는다.
3. 원인을 발견 실패, 결정 정당성 실패, 생산 게이트 실패, 검증 실패로 분류하고 이를 허용한 발동·정보·판정 기준·도구를 추적한다.
4. 규칙을 적용한 결과가 목적을 해쳤다면 규칙 자체의 범위·방향·소속을 추적한다.
5. `~/.claude/harness-authoring.md`와 `~/.claude/rules/harness-authoring.md`, 대상 유형의 rule을 읽는다. 개선 대상이 자기수정 구조면 `~/.claude/self-harness-architecture.md`도 읽는다.

## 개입

다음 실행에도 남으면서 전체 실패 경로의 원인을 직접 바꾸는 가장 작은 개입을 고른다. 소속과 위치는 `~/.claude/rules/harness-authoring.md`에서 정한다. skill이 존재했는데 발동하지 않았다면 본문보다 description을 먼저 검사하고, 여러 구성물이 같은 단계를 소유하면 책임을 분리한다. 새 구성물은 기존 책임으로 원인을 바꿀 수 없을 때만 추가한다.

## 변경과 검증

1. 원인과 최소 수정안을 사용자에게 제안하고 승인받는다.
2. 승인된 Claude Code 또는 shared 문서를 편집한다.
3. 사건마다 이전 agent를 재사용하지 않는 새 `meta-doc-critic`에 대상 경로·원 사건·직접 연결된 metadata workflow만 전달한다. 작성자의 결론이나 기대 답은 넘기지 않고, critic은 한 번만 피드백한다. agent를 사용할 수 없으면 분리된 읽기 전용 검수 패스를 한 번 수행하고 한계를 밝힌다.
4. 지적을 반영한 뒤 작성자가 지적별 수정 여부와 직접 회귀를 대조하고 종료한다. 자동 재검수는 하지 않는다. 수정이 계약을 다시 바꾸거나 직접 판정하기 어려우면 이유와 범위를 사용자에게 제시하고 승인받은 경우에만 별도의 fresh critic을 사용한다.
5. 원인이 Codex에도 존재할 구조적 근거가 있을 때만 `port-harness-change`를 사용한다. 모델 성향이나 Claude Code 기능에 묶인 문제는 Claude Code에 남긴다.
