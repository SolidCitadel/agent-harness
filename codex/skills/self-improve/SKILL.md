---
name: self-improve
description: 요구의 임의 해석, 필요한 조사·질문·검수·기존 상태·권한·완료 근거 확인의 누락, 또는 작업 방식·지침 적용으로 목적이 훼손됐다는 지적을 받았을 때 최초 근거 없는 확정과 누락된 게이트를 추적해 담당 하네스를 개선한다.
---

# 자기 개선

## 진단

1. 원 사건과 관련 문서·도구의 실제 상태를 대조한다.
2. 결과가 잘못된 마지막 시점이 아니라 최초로 근거 없는 결정을 확정한 시점을 찾는다.
3. 원인이 발견·결정 정당성·생산 게이트·검증 중 어느 실패인지 분류하고 이를 허용한 발동·정보·판정 기준·도구를 추적한다.
4. 같은 원인이 재발할 범위를 현재 작업·경로, 프로젝트, Codex 전역, 플랫폼 공통으로 판정한다. 원인과 동일한 실패 경로가 확인되지 않은 범위로 넓히지 않는다.
5. 판정한 범위에서 변경을 소비할 하네스 구성물과 소유 문서를 정한다.
	- 현재 작업에만 한정된 원인은 하네스를 바꾸지 않고 작업 안에서 교정한다.
	- 경로·프로젝트 범위는 해당 범위에 실제로 로드되는 가장 좁은 프로젝트 소유자를 고른다.
	- Codex 전역 또는 플랫폼 공통 범위는 `~/.codex/harness-authoring.md`·`~/.codex/harness-components.md`·`~/.codex/self-harness-architecture.md`를 읽고 소유자를 정한다.

## 개입

1. 선택한 재발 범위에서 원인을 제거하는 변경과 같은 원인 사례의 처분을 설계한다.
2. 발동 실패는 description, 정보 부족은 로드되는 지침·참조, 잘못된 판정은 판정 기준, 생산·검증 실패는 해당 workflow·검사에서 교정한다.

## 변경과 검증

1. 원인·재발 범위·변경 소유자·수정안·검증 대상을 사용자에게 제안하고 승인받는다.
2. 승인된 소유 문서를 편집한다. 처음부터 플랫폼 공통 범위로 판정한 변경은 한 플랫폼을 먼저 고치지 않고 shared 소유자를 직접 편집한다.
3. 사건마다 부모 이력을 상속하지 않는 새 `meta_doc_critic`에 대상 메타 문서·원 사건·직접 연결된 workflow만 전달하고 작성자의 결론이나 기대 답은 제외한다. agent를 쓸 수 없으면 분리된 읽기 전용 검수를 한 번 수행하고 한계를 밝힌다.
4. critic 지적이 승인된 재발 범위·소유자·수정안·검증 대상 안에 있을 때만 반영한다. 하나라도 달라지면 수정 전에 다시 승인받는다. 반영 뒤 범위 안의 같은 원인 사례가 차단되고 범위 밖의 정당한 사례가 보존되는지 직접 대조하며, 직접 판정하기 어려울 때만 이유와 범위를 제시해 별도의 fresh critic을 사용한다.
5. Codex 범위로 완료한 변경 뒤 Claude Code에도 같은 실패 경로가 있음을 새로 확인한 경우에만 `port-harness-change`를 사용한다.
